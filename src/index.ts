/**
 * superandom public entry point.
 *
 * Importing this module has no side effects: it installs no globals, attaches no
 * listeners and issues no requests. Call `create()` to start collecting.
 */

export const VERSION = '1.0.0';
